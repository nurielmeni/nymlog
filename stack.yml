version: '3.4'
services:
  mongo:
    image: mongo
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: nymlog
    volumes:
      - db-data:/etc/mongo
    networks:
      - nymlognet
  nymlog:
    image: nurielmeni/nymlog
    user: "node"
    working_dir: /home/node/app
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/home/node/app
    expose:
      - "8081"
    command: "npm start"
    networks:
      - nymlognet
networks:
  nymlognet:
volumes:
  db-data: